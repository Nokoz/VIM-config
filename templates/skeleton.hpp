%s/<++>/\=expand('%:t:r')/g
#ifndef <++>_HPP
# define <++>_HPP

// Includes
# include <iostream>
# include <string>

// Class declaration
class <++> {
public:
    // Constructors
    <++>(void);
    // Destructor
    ~<++>(void);

    // Public member functions
    void someFunction();
    
private:
    // Private member variables
    string <++>;
	
};

#endif // <++>_HPP
